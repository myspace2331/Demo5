EXP2
To design a flutter UI by including common widgets
( To develop a flutter UI for creating student-ID using basic widgets of
flutter.)

import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          title: Text('TE-IT ID Card'),
          backgroundColor: Color(0xfffaecff),
        ),
        body: Center(
          child: Card(
            elevation: 10,
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(15),
            ),
            child: Padding(
              padding: EdgeInsets.symmetric(horizontal: 30, vertical: 20),
              child: Column(
                mainAxisSize: MainAxisSize.min,
                children: [
                  CircleAvatar(
                    radius: 50,
                    backgroundImage: AssetImage('assets/images/thumb.jpeg'),
                  ),
                  SizedBox(height: 10),
                  Text(
                    'Sayyeda Rumiza',
                    style: TextStyle(
                      fontSize: 20,
                      fontWeight: FontWeight.bold,
                    ),
                  ),
                  SizedBox(height: 5),
                  Text('Moodle Id:24204005'),
                  SizedBox(height: 5),
                  Text('Department: Information Tachnology'),
                  SizedBox(height: 5),
                  Text('A. P. Shah Institude of Technology'),
                ],
              ),
            ),
          ),
        ),
      ),
    );
  }
}